//Aqui será feito o reset estético da página por meio do styled components

import { createGlobalStyle } from "styled-components";

const EstiloGlobal = createGlobalStyle`
  * {
    margin: 0;
    padding: 0;
  }
`

export default EstiloGlobal
